# Migration 036 — Skipped

This migration number was intentionally skipped during development.
Migration 035 (`service_admin_roles`) is followed by 037 (`lawyer_access_fee_commissions`).

If you need to insert a migration between these two, rename this file and add your SQL.
Otherwise, leave it as a placeholder to acknowledge the gap.
